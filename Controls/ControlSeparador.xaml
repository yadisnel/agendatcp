<UserControl x:Class="Agenda.Controles.ControlSeparador"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controles="clr-namespace:Agenda.Controles"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="6" Height="Auto" Width="1" SnapsToDevicePixels="True" DataContext="{Binding RelativeSource={RelativeSource Self}}">    
            <Grid Width="Auto" Height="Auto" VerticalAlignment="Stretch" HorizontalAlignment="Center" >
                <Border Name="BordeUnico" Width="1" BorderThickness="1" Margin="0,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Stretch" Height="Auto" BorderBrush="#FFCDD1DB"/>
            </Grid>      
</UserControl> 
